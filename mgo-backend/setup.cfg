[importlinter]
root_package = app

[importlinter:contract:modules-independent]
name = Modules must not import each other directly
type = independence
modules =
    app.modules.identity
    app.modules.billing
    app.modules.knowledge
    app.modules.embedding
    app.modules.retrieval
    app.modules.agent
    app.modules.chat
    app.modules.platform